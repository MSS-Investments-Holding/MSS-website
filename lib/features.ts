export const NEWS_ENABLED = process.env.NEXT_PUBLIC_ENABLE_NEWS === "true";

// Hiring paused — hides the Open Roles listing and the job detail pages.
// Flip to true to restore the careers listings when hiring resumes.
export const HIRING_ENABLED = false;
